import { Roboto, Montserrat, Inria_Sans } from 'next/font/google';

export const roboto = Roboto({ subsets: ['latin'], weight: ['400', '700'] });

export const montserrat = Montserrat({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const inria = Inria_Sans({
  subsets: ['latin'],
  weight: ['400', '700'],
});
